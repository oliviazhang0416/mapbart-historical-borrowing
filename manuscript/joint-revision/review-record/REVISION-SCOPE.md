# Governing scope after user correction

The user clarified that the current method jointly models the treatment arm and that substantial revision is necessary. The compact coauthor organization is a layout base, not the scientific primary-model specification.

## Accepted invariants and primary evidence
- Primary Gaussian likelihood: Y = f(X) + S g(X) + A psi + error; S marks any trial participant; A marks treated trial participants. Both trial arms share a residual variance. The common treatment coefficient has Normal(0,100) prior.
- Existing frozen 200-dataset, 5-condition, 3-method, 3-chain study supplies joint-model evidence. No new fits or favorable-case selection are authorized by the rewrite itself. Include all conditions and convergence qualifications.
- Separate-treatment Gaussian/survival 100-replicate study and n230 single-arm application must be explicitly separate analyses. Never relabel old n283 hazard results as current n230 results.
- ESS is a prior-information reference about a specified trial-control mean, based on external-only f plus prior g. It is not an established borrowing ESS for psi in the joint posterior. Use all trial profiles as the fixed target to match the implementation.
- Preserve source-dependent values, authors blank, coauthor baseline and prior local work. No claim of broad superiority, interaction recovery, null calibration or submission readiness from incomplete evidence.

## Planned exposition
Joint model and estimand first; forest/discrepancy/treatment priors; joint posterior computation; control-mean information reference; explicitly scoped joint fixed-region learning and source-control risk benchmark; primary joint experiment; separate-treatment/single-arm illustration; discussion distinguishing achieved results and remaining evidence.

## Submission standard
Complete scientific and artifact QA now. Record unresolved convergence, comparator/variance ablations, calibration alignment and primary-method application needs in an external readiness note. Do not invent analyses or hide these limitations through prose.
