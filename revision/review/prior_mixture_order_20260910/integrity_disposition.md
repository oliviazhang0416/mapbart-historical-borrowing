# Scope and mathematical integrity

Equation (2) is the sole numbered-equation difference. The user authorized replacing its indicator-conditioned normal by the explicit two-component mixture. Marginalizing a Bernoulli(w) indicator gives weights w and 1-w. The same tau_{1-z}^2 representation remains defined in prose and remains unchanged in the appendix and sampler description.

For fixed x, exactly one leaf from each of H_g trees contributes. Conditional on f, T_g, w and the scales, these contributions are independent. An indicator configuration with m spikes has probability w^m(1-w)^(H_g-m), and its normal sum has mean f(x) and variance m*tau_0^2+(H_g-m)*tau_1^2. There are choose(H_g,m) such configurations. Grouping them yields the displayed H_g+1-component mixture; the binomial theorem makes its weights sum to one. The m=H_g and m=0 components recover the previous all-spike and all-slab conditional priors. The mixture is conditional on w and the scales, which have not been integrated out. This calculation is pointwise and leaves the standardized-mean weighted-leaf law unchanged.

The separate g-sum, all-spike, all-slab and indicator-conditioned f_1 displays are replaced by an inline definition of g and one induced-mixture display. No empirical values or hyperparameters change. The longer inline conditional is stated in words to avoid overflow; its variance remains explicitly indexed by 1-z. The hyperprior sentence is split for layout. The Student-t qualification and everything after it are byte-identical to the baseline; everything preceding the pooled-surface paragraph is also byte-identical.

The checker passes citations, labels, sections, references, graphics and protected blocks. Its equation-preservation failure is the authorized equation (2) replacement. Inline-math and numeric token deltas map to the mixture count/weights and removal of duplicate displays; no reported result changes. Full source diff and raw checker output are archived locally.
